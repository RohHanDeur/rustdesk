name: Build Windows RustDesk (GUI + MSI)

on:
  push:
    branches: [ master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-2022

    steps:
      # ------------------------------------------------------------
      # 1) Checkout source (with submodules)
      # ------------------------------------------------------------
      - name: Checkout source
        uses: actions/checkout@v4
        with:
          submodules: recursive

      # ------------------------------------------------------------
      # 2) Install Rust
      # ------------------------------------------------------------
      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      # ------------------------------------------------------------
      # 3) Setup MSBuild
      # ------------------------------------------------------------
      - name: Setup MSBuild
        uses: microsoft/setup-msbuild@v2

      # ------------------------------------------------------------
      # 4) Install Python
      # ------------------------------------------------------------
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      # ------------------------------------------------------------
      # 5) Install vcpkg (no opus)
      # ------------------------------------------------------------
      - name: Install vcpkg
        shell: pwsh
        run: |
          $vcpkgPath = Join-Path $env:RUNNER_TEMP "vcpkg"
          git clone https://github.com/microsoft/vcpkg $vcpkgPath
          & "$vcpkgPath\bootstrap-vcpkg.bat"

          "VCPKG_ROOT=$vcpkgPath" | Out-File -FilePath $env:GITHUB_ENV -Append
          "VCPKG_DEFAULT_TRIPLET=x64-windows-static" | Out-File -FilePath $env:GITHUB_ENV -Append

      # ------------------------------------------------------------
      # 6) Remove opus from vcpkg.json (RustDesk uses FFmpeg-built opus)
      # ------------------------------------------------------------
      - name: Remove opus dependency
        shell: pwsh
        run: |
          if (Test-Path "vcpkg.json") {
            $json = Get-Content "vcpkg.json" -Raw | ConvertFrom-Json
            $json.dependencies = @($json.dependencies | Where-Object { $_ -ne "opus" })
            $json | ConvertTo-Json -Depth 10 | Set-Content "vcpkg.json" -Encoding UTF8
            Write-Host "opus removed from vcpkg.json"
          }

      # ------------------------------------------------------------
      # 7) Run vcpkg install
      # ------------------------------------------------------------
      - name: Run vcpkg install
        shell: pwsh
        run: |
          $vcpkg = Join-Path $env:VCPKG_ROOT "vcpkg.exe"
          Push-Location "$env:GITHUB_WORKSPACE"
          & $vcpkg install --triplet x64-windows-static
          Pop-Location

      # ------------------------------------------------------------
      # 8) Disable portable build in build.py
      # ------------------------------------------------------------
      - name: Disable portable build in build.py
        shell: pwsh
        run: |
          $file = "build.py"
          if (Test-Path $file) {
            Write-Host "Patching build.py to disable portable pack..."
            (Get-Content $file) `
              -replace "def build_portable[\\s\\S]*?def", "def build_portable(*args, **kwargs):`n    return`n`ndef" `
              -replace "if options\\.portable[\\s\\S]*?pass", "# portable disabled in CI" `
              | Set-Content $file -Encoding UTF8
          }

      # ------------------------------------------------------------
      # 9) Build Windows with FFmpeg (important!)
      # ------------------------------------------------------------
      - name: Build RustDesk with FFmpeg (hwcodec)
        shell: pwsh
        run: |
          python build.py --hwcodec --skip-portable-pack

      # ------------------------------------------------------------
      # 10) Install WiX Toolset
      # ------------------------------------------------------------
      - name: Install WiX Toolset
        shell: pwsh
        run: |
          $wix = "$env:ProgramFiles\WixToolsetV3"
          New-Item -ItemType Directory -Force -Path $wix | Out-Null
          Invoke-WebRequest -Uri "https://wixtoolset.org/releases/v3.11.2/wix311-binaries.zip" -OutFile wix.zip
          Expand-Archive wix.zip -DestinationPath $wix -Force
          $wixBin = Join-Path $wix "bin"
          $wixBin | Out-File -FilePath $env:GITHUB_PATH -Append

      # ------------------------------------------------------------
      # 11) Build MSI installer
      # ------------------------------------------------------------
      - name: Build Windows MSI
        shell: pwsh
        run: |
          powershell ./build.ps1 -Configuration Release

      # ------------------------------------------------------------
      # 12) Upload artifacts
      # ------------------------------------------------------------
      - name: Upload EXE + MSI
        uses: actions/upload-artifact@v4
        with:
          name: rustdesk-windows-build
          path: |
            target\release\rustdesk.exe
            target\release\*.msi